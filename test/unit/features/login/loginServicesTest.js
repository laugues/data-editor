'use strict';

/* jasmine specs for controllers go here */
describe('LoginServices', function () {

    beforeEach(module('itesoftMlEditor'));


    describe('check the existence of LoginService factory', function () {
        // Test service availability
        it('check the existence of Login factory', inject(function () {
            //expect(LoginService).toBeDefined();
        }));
    });

});
